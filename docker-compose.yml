version: '3.8'
services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"
  web:
    build: .
    environment:
      - REDIS_URL=redis://redis:6379/0
      - PORT=5000
    ports:
      - "5000:5000"
    depends_on:
      - redis
